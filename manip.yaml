path: "pasqualedem/weeddetection"
runs:
  -
    filters:
      group: RedEdge
#      config.in_params.dataset.channels: ["NDVI"]
    updated_config:
      null
#      train_params:
#        freeze_pretrained: False
    updated_meta:
    keys_to_delete:
      null