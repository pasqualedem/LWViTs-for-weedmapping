path: "pasqualedem/weeddetection"
runs:
  -
    fix_string_params: False
    filters:
      $or:
      - group: Lawin
      - group: Focal
      - group: LawinPretrained
      - group: LawinAugPatience
      - group: RRotationPatience
      - group: SplitLaweedFrozen
    updated_config:
    updated_meta:
    keys_to_delete:
    files_to_delete:
    artifacts_to_delete:
      "lambda a: 'test_seg' in a.name"

#    filters:
#      group: Laweed
#      config.in_params.dataset.channels: ["NDVI"]
#    updated_config:
#      train_params:
#        freeze_pretrained: False
#    files_to_delete:
#      ['ckpt_best.pth', 'ckpt_latest.pth']
#      "lambda f: f.name.endswith('.png')"